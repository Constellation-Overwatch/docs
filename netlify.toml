[build]
  command = "bun run build"
  publish = "dist/client"

[dev]
  command = "bun dev"
  targetPort = 3003
  port = 3000

[functions]
  directory = ".netlify/v1/functions"
  included_files = ["content/docs/**"]

# Let the Netlify plugin handle all routing automatically